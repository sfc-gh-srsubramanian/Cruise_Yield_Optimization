-- =============================================================================
-- {{SOLUTION_TITLE}} - ML Objects
-- Layer: ML (Machine Learning)
-- Description: Feature table and staging tables for ML models.
--              Models are trained and UDFs created in 14_ml_training.sql.
-- =============================================================================

USE SCHEMA {{DATABASE_NAME}}.ML;

-- Feature table for ML pricing model (populated in 11_load_data.sql)
CREATE OR REPLACE TABLE PRICING_FEATURES (
    EVENT_ID VARCHAR(20),
    GUEST_ID VARCHAR(20),
    SAILING_ID VARCHAR(10),
    CABIN_CLASS VARCHAR(30),
    OFFERED_PRICE NUMBER(10,2),
    CONVERTED NUMBER(1,0),
    LOYALTY_TIER VARCHAR(20),
    LOYALTY_TIER_NUM NUMBER(1,0),
    PRICE_SENSITIVITY NUMBER(4,2),
    PAST_CRUISES NUMBER(38,0),
    LIFETIME_VALUE NUMBER(12,2),
    AVG_ONBOARD_SPEND NUMBER(10,2),
    SEASON_MULTIPLIER NUMBER(4,2),
    DURATION_NIGHTS NUMBER(38,0),
    ITINERARY_REGION VARCHAR(50),
    BASE_PRICE NUMBER(4,0),
    PRICE_RATIO NUMBER(16,8)
);

-- Staging tables for time-series models (populated in 14_ml_training.sql)
CREATE OR REPLACE TABLE REVENUE_TIMESERIES (
    WEEK_START DATE,
    SERIES_ID VARCHAR(50),
    WEEKLY_REVENUE NUMBER(14,2)
);

CREATE OR REPLACE TABLE PRICING_TIMESERIES (
    WEEK_START DATE,
    SERIES_ID VARCHAR(50),
    AVG_OFFERED_PRICE NUMBER(10,2),
    CONVERSION_RATE NUMBER(10,6),
    EVENT_COUNT NUMBER(38,0)
);
